apiVersion: v1
kind: Service
metadata:
  name: pedidos
  namespace: pedidos-veloz
  labels:
    app: pedidos
spec:
  type: ClusterIP
  selector:
    app: pedidos
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
