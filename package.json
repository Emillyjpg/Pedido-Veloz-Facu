{
  "name": "pedidos-veloz",
  "version": "1.0.0",
  "private": true,
  "description": "Microsserviços de pedidos com Node.js, Express, Docker, K8s, Terraform e observabilidade",
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "lint": "eslint 'services/**/*.js'",
    "test": "jest --passWithNoTests",
    "test:ci": "jest --ci --coverage --passWithNoTests"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
